import CurrencyConverter from "../components/CurrencyConverter"

export const metadata = { title: "Currency Converter — Rubiks FX" }

export default function CurrencyPage() {
  return <CurrencyConverter />
}
